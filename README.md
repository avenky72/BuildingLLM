#Build an LLM that is comparable to GPT-2

Requirements:
- Python 3.10+
- PyTorch
- tiktoken
- tensorflow (for loading OpenAI weights)
- tqdm
- matplotlib
- numpy

then just run the cells in the notebook in order

after finetuning and also trying OpenAI's weights, the models follows instructions and answers questions
not always accurate due to limited model size (124M parameters)
